# CI run e168da58d93b
result: SUCCESS

## compileall output (last 100 lines)

## pytest output (last 400 lines)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
<string>:27: in __init__
    ???
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SecurityToolDefinition(tool_id='unit.tool', canonical_name='Unit Tool', category=<ToolCategory.SECURITY_RESOURCES: 'SE...e, dry_run_support=True, evidence_support=True, provenance='OWNER_CURATED', version='1.0.0', adapter_version='unbuilt')

    def __post_init__(self) -> None:
        tool_id = str(self.tool_id or "").strip()
        if not tool_id or not TOOL_ID_PATTERN.match(tool_id):
            raise ToolInventoryError("invalid tool_id: " + str(self.tool_id))
        object.__setattr__(self, "tool_id", tool_id)
        canonical_name = str(self.canonical_name or "").strip()
        if not canonical_name:
            raise ToolInventoryError("tool " + tool_id + " requires a canonical name")
        object.__setattr__(self, "canonical_name", canonical_name)
        description = str(self.description or "").strip()
        if not description:
            raise ToolInventoryError("tool " + tool_id + " requires a description")
        object.__setattr__(self, "description", description)
        for name in ("required_runtime", "platform"):
            if not str(getattr(self, name) or "").strip():
                raise ToolInventoryError("tool " + tool_id + " requires " + name)
        object.__setattr__(self, "category", _coerce_enum(self.category, ToolCategory, "tool " + tool_id + " category"))
        risk = _coerce_enum(self.risk_class, ToolRiskClass, "tool " + tool_id + " risk class")
        object.__setattr__(self, "risk_class", risk)
        object.__setattr__(self, "availability", _coerce_enum(self.availability, ToolAvailability, "tool " + tool_id + " availability"))
        object.__setattr__(self, "filesystem_access", _coerce_enum(self.filesystem_access, ToolAccessLevel, "tool " + tool_id + " filesystem access"))
        object.__setattr__(self, "process_execution", _coerce_enum(self.process_execution, ToolAccessLevel, "tool " + tool_id + " process execution"))
    
        # INV-TOOL-3: the authorization class is derived deterministically
        # from the risk class; nothing (and no one) can decouple them.
        expected_auth = RISK_AUTHORIZATION_CLASS[risk]
        supplied_auth = _coerce_enum(self.authorization_class, ToolAuthorizationClass, "tool " + tool_id + " authorization class")
        if supplied_auth is not expected_auth:
            raise ToolInventoryError(
                "tool " + tool_id + " authorization class must be the deterministic mapping of its risk class: "
                + risk.value + " -> " + expected_auth.value
            )
        object.__setattr__(self, "authorization_class", expected_auth)
    
        if not self.capabilities:
            raise ToolInventoryError("tool " + tool_id + " requires at least one capability")
        capabilities: list[ToolCapability] = []
        for item in self.capabilities:
            capability = _coerce_enum(item, ToolCapability, "tool " + tool_id + " capability")
            if capability in capabilities:
                raise ToolInventoryError("duplicate capability for tool " + tool_id + ": " + capability.value)
            capabilities.append(capability)
        object.__setattr__(self, "capabilities", tuple(capabilities))
    
        # INV-TOOL-4: lab-only default for offensive risk classes.
        if risk in OFFENSIVE_RISK_CLASSES and not self.lab_only:
>           raise ToolInventoryError(
                "tool " + tool_id + " has risk class " + risk.value + " and must be defined lab_only=True"
            )
E           security.tool_inventory.ToolInventoryError: tool unit.tool has risk class HIGH_RISK and must be defined lab_only=True

security/tool_inventory.py:297: ToolInventoryError
_ test_stage_a_authorization_class_must_match_risk_class[LAB_SECURITY_TESTING-LAB_AUTHORIZED_TARGET_REQUIRED] _

risk = <ToolRiskClass.LAB_SECURITY_TESTING: 'LAB_SECURITY_TESTING'>
expected = <ToolAuthorizationClass.LAB_AUTHORIZED_TARGET_REQUIRED: 'LAB_AUTHORIZED_TARGET_REQUIRED'>

    @pytest.mark.parametrize("risk,expected", sorted(
        ((risk, auth) for risk, auth in RISK_AUTHORIZATION_CLASS.items()),
        key=lambda pair: pair[0].value,
    ))
    def test_stage_a_authorization_class_must_match_risk_class(risk, expected):
        # A supplied class that disagrees with the risk class fails closed.
        wrong = next(auth for auth in ToolAuthorizationClass if auth is not expected)
        with pytest.raises(ToolInventoryError):
            _minimal_definition(risk_class=risk, authorization_class=wrong)
        # The matching class is accepted and stored.
>       definition = _minimal_definition(risk_class=risk, authorization_class=expected)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_security_tool_inventory.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_security_tool_inventory.py:71: in _minimal_definition
    return SecurityToolDefinition(**values)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
<string>:27: in __init__
    ???
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SecurityToolDefinition(tool_id='unit.tool', canonical_name='Unit Tool', category=<ToolCategory.SECURITY_RESOURCES: 'SE...e, dry_run_support=True, evidence_support=True, provenance='OWNER_CURATED', version='1.0.0', adapter_version='unbuilt')

    def __post_init__(self) -> None:
        tool_id = str(self.tool_id or "").strip()
        if not tool_id or not TOOL_ID_PATTERN.match(tool_id):
            raise ToolInventoryError("invalid tool_id: " + str(self.tool_id))
        object.__setattr__(self, "tool_id", tool_id)
        canonical_name = str(self.canonical_name or "").strip()
        if not canonical_name:
            raise ToolInventoryError("tool " + tool_id + " requires a canonical name")
        object.__setattr__(self, "canonical_name", canonical_name)
        description = str(self.description or "").strip()
        if not description:
            raise ToolInventoryError("tool " + tool_id + " requires a description")
        object.__setattr__(self, "description", description)
        for name in ("required_runtime", "platform"):
            if not str(getattr(self, name) or "").strip():
                raise ToolInventoryError("tool " + tool_id + " requires " + name)
        object.__setattr__(self, "category", _coerce_enum(self.category, ToolCategory, "tool " + tool_id + " category"))
        risk = _coerce_enum(self.risk_class, ToolRiskClass, "tool " + tool_id + " risk class")
        object.__setattr__(self, "risk_class", risk)
        object.__setattr__(self, "availability", _coerce_enum(self.availability, ToolAvailability, "tool " + tool_id + " availability"))
        object.__setattr__(self, "filesystem_access", _coerce_enum(self.filesystem_access, ToolAccessLevel, "tool " + tool_id + " filesystem access"))
        object.__setattr__(self, "process_execution", _coerce_enum(self.process_execution, ToolAccessLevel, "tool " + tool_id + " process execution"))
    
        # INV-TOOL-3: the authorization class is derived deterministically
        # from the risk class; nothing (and no one) can decouple them.
        expected_auth = RISK_AUTHORIZATION_CLASS[risk]
        supplied_auth = _coerce_enum(self.authorization_class, ToolAuthorizationClass, "tool " + tool_id + " authorization class")
        if supplied_auth is not expected_auth:
            raise ToolInventoryError(
                "tool " + tool_id + " authorization class must be the deterministic mapping of its risk class: "
                + risk.value + " -> " + expected_auth.value
            )
        object.__setattr__(self, "authorization_class", expected_auth)
    
        if not self.capabilities:
            raise ToolInventoryError("tool " + tool_id + " requires at least one capability")
        capabilities: list[ToolCapability] = []
        for item in self.capabilities:
            capability = _coerce_enum(item, ToolCapability, "tool " + tool_id + " capability")
            if capability in capabilities:
                raise ToolInventoryError("duplicate capability for tool " + tool_id + ": " + capability.value)
            capabilities.append(capability)
        object.__setattr__(self, "capabilities", tuple(capabilities))
    
        # INV-TOOL-4: lab-only default for offensive risk classes.
        if risk in OFFENSIVE_RISK_CLASSES and not self.lab_only:
>           raise ToolInventoryError(
                "tool " + tool_id + " has risk class " + risk.value + " and must be defined lab_only=True"
            )
E           security.tool_inventory.ToolInventoryError: tool unit.tool has risk class LAB_SECURITY_TESTING and must be defined lab_only=True

security/tool_inventory.py:297: ToolInventoryError
_ test_stage_a_authorization_class_must_match_risk_class[PRIVILEGED_SECURITY-PRIVILEGED_OWNER_APPROVAL] _

risk = <ToolRiskClass.PRIVILEGED_SECURITY: 'PRIVILEGED_SECURITY'>
expected = <ToolAuthorizationClass.PRIVILEGED_OWNER_APPROVAL: 'PRIVILEGED_OWNER_APPROVAL'>

    @pytest.mark.parametrize("risk,expected", sorted(
        ((risk, auth) for risk, auth in RISK_AUTHORIZATION_CLASS.items()),
        key=lambda pair: pair[0].value,
    ))
    def test_stage_a_authorization_class_must_match_risk_class(risk, expected):
        # A supplied class that disagrees with the risk class fails closed.
        wrong = next(auth for auth in ToolAuthorizationClass if auth is not expected)
        with pytest.raises(ToolInventoryError):
            _minimal_definition(risk_class=risk, authorization_class=wrong)
        # The matching class is accepted and stored.
>       definition = _minimal_definition(risk_class=risk, authorization_class=expected)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_security_tool_inventory.py:100: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_security_tool_inventory.py:71: in _minimal_definition
    return SecurityToolDefinition(**values)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
<string>:27: in __init__
    ???
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SecurityToolDefinition(tool_id='unit.tool', canonical_name='Unit Tool', category=<ToolCategory.SECURITY_RESOURCES: 'SE...e, dry_run_support=True, evidence_support=True, provenance='OWNER_CURATED', version='1.0.0', adapter_version='unbuilt')

    def __post_init__(self) -> None:
        tool_id = str(self.tool_id or "").strip()
        if not tool_id or not TOOL_ID_PATTERN.match(tool_id):
            raise ToolInventoryError("invalid tool_id: " + str(self.tool_id))
        object.__setattr__(self, "tool_id", tool_id)
        canonical_name = str(self.canonical_name or "").strip()
        if not canonical_name:
            raise ToolInventoryError("tool " + tool_id + " requires a canonical name")
        object.__setattr__(self, "canonical_name", canonical_name)
        description = str(self.description or "").strip()
        if not description:
            raise ToolInventoryError("tool " + tool_id + " requires a description")
        object.__setattr__(self, "description", description)
        for name in ("required_runtime", "platform"):
            if not str(getattr(self, name) or "").strip():
                raise ToolInventoryError("tool " + tool_id + " requires " + name)
        object.__setattr__(self, "category", _coerce_enum(self.category, ToolCategory, "tool " + tool_id + " category"))
        risk = _coerce_enum(self.risk_class, ToolRiskClass, "tool " + tool_id + " risk class")
        object.__setattr__(self, "risk_class", risk)
        object.__setattr__(self, "availability", _coerce_enum(self.availability, ToolAvailability, "tool " + tool_id + " availability"))
        object.__setattr__(self, "filesystem_access", _coerce_enum(self.filesystem_access, ToolAccessLevel, "tool " + tool_id + " filesystem access"))
        object.__setattr__(self, "process_execution", _coerce_enum(self.process_execution, ToolAccessLevel, "tool " + tool_id + " process execution"))
    
        # INV-TOOL-3: the authorization class is derived deterministically
        # from the risk class; nothing (and no one) can decouple them.
        expected_auth = RISK_AUTHORIZATION_CLASS[risk]
        supplied_auth = _coerce_enum(self.authorization_class, ToolAuthorizationClass, "tool " + tool_id + " authorization class")
        if supplied_auth is not expected_auth:
            raise ToolInventoryError(
                "tool " + tool_id + " authorization class must be the deterministic mapping of its risk class: "
                + risk.value + " -> " + expected_auth.value
            )
        object.__setattr__(self, "authorization_class", expected_auth)
    
        if not self.capabilities:
            raise ToolInventoryError("tool " + tool_id + " requires at least one capability")
        capabilities: list[ToolCapability] = []
        for item in self.capabilities:
            capability = _coerce_enum(item, ToolCapability, "tool " + tool_id + " capability")
            if capability in capabilities:
                raise ToolInventoryError("duplicate capability for tool " + tool_id + ": " + capability.value)
            capabilities.append(capability)
        object.__setattr__(self, "capabilities", tuple(capabilities))
    
        # INV-TOOL-4: lab-only default for offensive risk classes.
        if risk in OFFENSIVE_RISK_CLASSES and not self.lab_only:
>           raise ToolInventoryError(
                "tool " + tool_id + " has risk class " + risk.value + " and must be defined lab_only=True"
            )
E           security.tool_inventory.ToolInventoryError: tool unit.tool has risk class PRIVILEGED_SECURITY and must be defined lab_only=True

security/tool_inventory.py:297: ToolInventoryError
_ test_stage_a_non_offensive_risk_classes_are_not_lab_only_by_default[AUTHORIZED_SCANNING] _

risk = <ToolRiskClass.AUTHORIZED_SCANNING: 'AUTHORIZED_SCANNING'>

    @pytest.mark.parametrize("risk", sorted(
        set(ToolRiskClass) - tool_inventory.OFFENSIVE_RISK_CLASSES, key=lambda r: r.value,
    ))
    def test_stage_a_non_offensive_risk_classes_are_not_lab_only_by_default(risk):
>       definition = _minimal_definition(risk_class=risk)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_security_tool_inventory.py:125: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_security_tool_inventory.py:71: in _minimal_definition
    return SecurityToolDefinition(**values)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
<string>:27: in __init__
    ???
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SecurityToolDefinition(tool_id='unit.tool', canonical_name='Unit Tool', category=<ToolCategory.SECURITY_RESOURCES: 'SE...e, dry_run_support=True, evidence_support=True, provenance='OWNER_CURATED', version='1.0.0', adapter_version='unbuilt')

    def __post_init__(self) -> None:
        tool_id = str(self.tool_id or "").strip()
        if not tool_id or not TOOL_ID_PATTERN.match(tool_id):
            raise ToolInventoryError("invalid tool_id: " + str(self.tool_id))
        object.__setattr__(self, "tool_id", tool_id)
        canonical_name = str(self.canonical_name or "").strip()
        if not canonical_name:
            raise ToolInventoryError("tool " + tool_id + " requires a canonical name")
        object.__setattr__(self, "canonical_name", canonical_name)
        description = str(self.description or "").strip()
        if not description:
            raise ToolInventoryError("tool " + tool_id + " requires a description")
        object.__setattr__(self, "description", description)
        for name in ("required_runtime", "platform"):
            if not str(getattr(self, name) or "").strip():
                raise ToolInventoryError("tool " + tool_id + " requires " + name)
        object.__setattr__(self, "category", _coerce_enum(self.category, ToolCategory, "tool " + tool_id + " category"))
        risk = _coerce_enum(self.risk_class, ToolRiskClass, "tool " + tool_id + " risk class")
        object.__setattr__(self, "risk_class", risk)
        object.__setattr__(self, "availability", _coerce_enum(self.availability, ToolAvailability, "tool " + tool_id + " availability"))
        object.__setattr__(self, "filesystem_access", _coerce_enum(self.filesystem_access, ToolAccessLevel, "tool " + tool_id + " filesystem access"))
        object.__setattr__(self, "process_execution", _coerce_enum(self.process_execution, ToolAccessLevel, "tool " + tool_id + " process execution"))
    
        # INV-TOOL-3: the authorization class is derived deterministically
        # from the risk class; nothing (and no one) can decouple them.
        expected_auth = RISK_AUTHORIZATION_CLASS[risk]
        supplied_auth = _coerce_enum(self.authorization_class, ToolAuthorizationClass, "tool " + tool_id + " authorization class")
        if supplied_auth is not expected_auth:
>           raise ToolInventoryError(
                "tool " + tool_id + " authorization class must be the deterministic mapping of its risk class: "
                + risk.value + " -> " + expected_auth.value
            )
E           security.tool_inventory.ToolInventoryError: tool unit.tool authorization class must be the deterministic mapping of its risk class: AUTHORIZED_SCANNING -> AUTHORIZED_SCOPE_REQUIRED

security/tool_inventory.py:279: ToolInventoryError
_ test_stage_a_non_offensive_risk_classes_are_not_lab_only_by_default[LOCAL_ANALYSIS] _

risk = <ToolRiskClass.LOCAL_ANALYSIS: 'LOCAL_ANALYSIS'>

    @pytest.mark.parametrize("risk", sorted(
        set(ToolRiskClass) - tool_inventory.OFFENSIVE_RISK_CLASSES, key=lambda r: r.value,
    ))
    def test_stage_a_non_offensive_risk_classes_are_not_lab_only_by_default(risk):
>       definition = _minimal_definition(risk_class=risk)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_security_tool_inventory.py:125: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_security_tool_inventory.py:71: in _minimal_definition
    return SecurityToolDefinition(**values)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
<string>:27: in __init__
    ???
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SecurityToolDefinition(tool_id='unit.tool', canonical_name='Unit Tool', category=<ToolCategory.SECURITY_RESOURCES: 'SE...e, dry_run_support=True, evidence_support=True, provenance='OWNER_CURATED', version='1.0.0', adapter_version='unbuilt')

    def __post_init__(self) -> None:
        tool_id = str(self.tool_id or "").strip()
        if not tool_id or not TOOL_ID_PATTERN.match(tool_id):
            raise ToolInventoryError("invalid tool_id: " + str(self.tool_id))
        object.__setattr__(self, "tool_id", tool_id)
        canonical_name = str(self.canonical_name or "").strip()
        if not canonical_name:
            raise ToolInventoryError("tool " + tool_id + " requires a canonical name")
        object.__setattr__(self, "canonical_name", canonical_name)
        description = str(self.description or "").strip()
        if not description:
            raise ToolInventoryError("tool " + tool_id + " requires a description")
        object.__setattr__(self, "description", description)
        for name in ("required_runtime", "platform"):
            if not str(getattr(self, name) or "").strip():
                raise ToolInventoryError("tool " + tool_id + " requires " + name)
        object.__setattr__(self, "category", _coerce_enum(self.category, ToolCategory, "tool " + tool_id + " category"))
        risk = _coerce_enum(self.risk_class, ToolRiskClass, "tool " + tool_id + " risk class")
        object.__setattr__(self, "risk_class", risk)
        object.__setattr__(self, "availability", _coerce_enum(self.availability, ToolAvailability, "tool " + tool_id + " availability"))
        object.__setattr__(self, "filesystem_access", _coerce_enum(self.filesystem_access, ToolAccessLevel, "tool " + tool_id + " filesystem access"))
        object.__setattr__(self, "process_execution", _coerce_enum(self.process_execution, ToolAccessLevel, "tool " + tool_id + " process execution"))
    
        # INV-TOOL-3: the authorization class is derived deterministically
        # from the risk class; nothing (and no one) can decouple them.
        expected_auth = RISK_AUTHORIZATION_CLASS[risk]
        supplied_auth = _coerce_enum(self.authorization_class, ToolAuthorizationClass, "tool " + tool_id + " authorization class")
        if supplied_auth is not expected_auth:
>           raise ToolInventoryError(
                "tool " + tool_id + " authorization class must be the deterministic mapping of its risk class: "
                + risk.value + " -> " + expected_auth.value
            )
E           security.tool_inventory.ToolInventoryError: tool unit.tool authorization class must be the deterministic mapping of its risk class: LOCAL_ANALYSIS -> LOCAL_DATA_ONLY

security/tool_inventory.py:279: ToolInventoryError
_ test_stage_a_non_offensive_risk_classes_are_not_lab_only_by_default[PASSIVE_RECON] _

risk = <ToolRiskClass.PASSIVE_RECON: 'PASSIVE_RECON'>

    @pytest.mark.parametrize("risk", sorted(
        set(ToolRiskClass) - tool_inventory.OFFENSIVE_RISK_CLASSES, key=lambda r: r.value,
    ))
    def test_stage_a_non_offensive_risk_classes_are_not_lab_only_by_default(risk):
>       definition = _minimal_definition(risk_class=risk)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_security_tool_inventory.py:125: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_security_tool_inventory.py:71: in _minimal_definition
    return SecurityToolDefinition(**values)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
<string>:27: in __init__
    ???
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SecurityToolDefinition(tool_id='unit.tool', canonical_name='Unit Tool', category=<ToolCategory.SECURITY_RESOURCES: 'SE...e, dry_run_support=True, evidence_support=True, provenance='OWNER_CURATED', version='1.0.0', adapter_version='unbuilt')

    def __post_init__(self) -> None:
        tool_id = str(self.tool_id or "").strip()
        if not tool_id or not TOOL_ID_PATTERN.match(tool_id):
            raise ToolInventoryError("invalid tool_id: " + str(self.tool_id))
        object.__setattr__(self, "tool_id", tool_id)
        canonical_name = str(self.canonical_name or "").strip()
        if not canonical_name:
            raise ToolInventoryError("tool " + tool_id + " requires a canonical name")
        object.__setattr__(self, "canonical_name", canonical_name)
        description = str(self.description or "").strip()
        if not description:
            raise ToolInventoryError("tool " + tool_id + " requires a description")
        object.__setattr__(self, "description", description)
        for name in ("required_runtime", "platform"):
            if not str(getattr(self, name) or "").strip():
                raise ToolInventoryError("tool " + tool_id + " requires " + name)
        object.__setattr__(self, "category", _coerce_enum(self.category, ToolCategory, "tool " + tool_id + " category"))
        risk = _coerce_enum(self.risk_class, ToolRiskClass, "tool " + tool_id + " risk class")
        object.__setattr__(self, "risk_class", risk)
        object.__setattr__(self, "availability", _coerce_enum(self.availability, ToolAvailability, "tool " + tool_id + " availability"))
        object.__setattr__(self, "filesystem_access", _coerce_enum(self.filesystem_access, ToolAccessLevel, "tool " + tool_id + " filesystem access"))
        object.__setattr__(self, "process_execution", _coerce_enum(self.process_execution, ToolAccessLevel, "tool " + tool_id + " process execution"))
    
        # INV-TOOL-3: the authorization class is derived deterministically
        # from the risk class; nothing (and no one) can decouple them.
        expected_auth = RISK_AUTHORIZATION_CLASS[risk]
        supplied_auth = _coerce_enum(self.authorization_class, ToolAuthorizationClass, "tool " + tool_id + " authorization class")
        if supplied_auth is not expected_auth:
>           raise ToolInventoryError(
                "tool " + tool_id + " authorization class must be the deterministic mapping of its risk class: "
                + risk.value + " -> " + expected_auth.value
            )
E           security.tool_inventory.ToolInventoryError: tool unit.tool authorization class must be the deterministic mapping of its risk class: PASSIVE_RECON -> OWNER_APPROVED_PASSIVE

security/tool_inventory.py:279: ToolInventoryError
=========================== short test summary info ============================
FAILED tests/test_security_tool_inventory.py::test_stage_a_authorization_class_must_match_risk_class[HIGH_RISK-LAB_AUTHORIZED_TARGET_REQUIRED] - security.tool_inventory.ToolInventoryError: tool unit.tool has risk class HIGH_RISK and must be defined lab_only=True
FAILED tests/test_security_tool_inventory.py::test_stage_a_authorization_class_must_match_risk_class[LAB_SECURITY_TESTING-LAB_AUTHORIZED_TARGET_REQUIRED] - security.tool_inventory.ToolInventoryError: tool unit.tool has risk class LAB_SECURITY_TESTING and must be defined lab_only=True
FAILED tests/test_security_tool_inventory.py::test_stage_a_authorization_class_must_match_risk_class[PRIVILEGED_SECURITY-PRIVILEGED_OWNER_APPROVAL] - security.tool_inventory.ToolInventoryError: tool unit.tool has risk class PRIVILEGED_SECURITY and must be defined lab_only=True
FAILED tests/test_security_tool_inventory.py::test_stage_a_non_offensive_risk_classes_are_not_lab_only_by_default[AUTHORIZED_SCANNING] - security.tool_inventory.ToolInventoryError: tool unit.tool authorization class must be the deterministic mapping of its risk class: AUTHORIZED_SCANNING -> AUTHORIZED_SCOPE_REQUIRED
FAILED tests/test_security_tool_inventory.py::test_stage_a_non_offensive_risk_classes_are_not_lab_only_by_default[LOCAL_ANALYSIS] - security.tool_inventory.ToolInventoryError: tool unit.tool authorization class must be the deterministic mapping of its risk class: LOCAL_ANALYSIS -> LOCAL_DATA_ONLY
FAILED tests/test_security_tool_inventory.py::test_stage_a_non_offensive_risk_classes_are_not_lab_only_by_default[PASSIVE_RECON] - security.tool_inventory.ToolInventoryError: tool unit.tool authorization class must be the deterministic mapping of its risk class: PASSIVE_RECON -> OWNER_APPROVED_PASSIVE
6 failed, 1080 passed, 1 skipped in 11.01s
